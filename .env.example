DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"
NODE_ENV="production"

# No Render: NÃO defina PORT manualmente — a plataforma injeta.
# Frontends (Vercel). Produção:
# CORS_ORIGIN="https://sporthub-frontend.vercel.app"
CORS_ORIGIN="https://sporthub-frontend.vercel.app"
